# Catálogo — corrección visual de tarjetas

- Las portadas tienen una altura uniforme y no pueden expandir la tarjeta.
- Las imágenes grandes se recortan visualmente con `object-fit: cover`, sin modificar el archivo original.
- Todas las tarjetas mantienen la misma estructura y altura.
- El estado Activo/Inactivo ya no se parte en varias líneas.
- Títulos, metadatos y botones quedan alineados.
- La corrección está aislada en `static/catalog-card-layout-fix.css`.
